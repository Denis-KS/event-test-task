import styled from "styled-components";
import { Box } from "./Box";

export const FormSectionBox = styled(Box).attrs(() => ({ backgroundColor: '#fff' }))`
    padding: 10px;
    margin: 15px 0 0 0;
`;